import React from "react"
import { useParams } from "react-router-dom";

export const ChangeElections = () => {
    const { username } = useParams();
    return (
        <div>
            <h1>Under construction</h1>
        </div>
    )
}